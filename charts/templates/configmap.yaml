apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "error-generator.fullname" . }}
  labels:
    {{- include "error-generator.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}